import java.util.*;
public class javaPracticeSetCwh {
//    static void isinarray(int[] arr,int k){
//        int n=arr.length;
//        for(int i=0;i<n;i++){
//            if(arr[i]==k){
//                System.out.println(i);
//            }
//        }
//    }
    /* static void avg(int[] arr, int n){
        int sum=0;
        for(int i=0;i<n;i++){
            sum += arr[i];
        }
        int avg=sum/n;
        System.out.println(avg);
    }

     */
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        /* question 1 -> Create an array of 5 floats and calculate their sum.
        int n=sc.nextInt();
        float[] arr=new float[n];
        float sum=0;
        for (int i=0;i<n;i++){
            arr[i]=sc.nextFloat();
            sum += arr[i];
        }
        System.out.println(sum);
         */

       /*question 2 -> Write a program to find out whether a given integer is present in an array or not.
        int n=sc.nextInt();
        int[] arr=new int[n];
        for (int i=0;i<n;i++) {
            arr[i] = sc.nextInt();
        }
         int k=sc.nextInt();
         isinarray(arr,k);

        */
        //question 3 -> Calculate the average marks from an array containing marks of all students in physics using a for-each loop.
        /* int n=sc.nextInt();
        int[] arr=new int[n];
        for (int i=0;i<n;i++) {
            arr[i] = sc.nextInt();
        }
        avg(arr,n);

         */

    }
}

